<%= simple_form_for [@list, @bookmark] do |f| %>
<%= f.input :movie_id, as: :select, collection: Movie.all, prompt: 'Select a movie'%>
<%= f.input :comment %>
<%= f.submit 'Add Bookmark' %>
<% end %>

<%= link_to 'Back to List', @list %>
